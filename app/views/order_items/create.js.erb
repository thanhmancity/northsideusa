<% if @order.errors.any? || @order_item.errors.any? %>
alert("not valid.")
<% else %>
  $(".cart-subtotal").html("<%= escape_javascript(render 'layouts/cart_subtotal') %>")
  window.location.replace("/cart");
<% end %>
